import { spawn } from 'node:child_process';
import { Readable } from 'node:stream';
import readline from 'node:readline';
import { mkdir, writeFile } from 'node:fs/promises';

type Side='BUY'|'SELL';
type Bar={time:number;open:number;high:number;low:number;close:number;volume:number;buyVolume:number;sellVolume:number};
type Cfg={holdMin:number;minVotes:number;minFamilies:number;lead:number;mom16Threshold:number};
type Stats={trades:number;winRate:number;pf:number;returnPct:number;ddPct:number;tradesPerDay:number;final50:number};
type Snap={buy:number;sell:number;buyFamilies:number;sellFamilies:number;atr:number;mom16:number};
type ConsensusCfg={minVotes:number;minFamilies:number;lead:number};
type Candidate={i:number;side:Side};
type R23Cfg=ConsensusCfg&{mom16Threshold:number;holdMin:number;targetPct:number;stopPct:number;reverse:boolean};

const SYMBOL='XAUUSDT',DAY=86400000,DAYS=Number(process.env.R26_DAYS||56),COST=.145,EXPOSURE=.10,ENTRY_MIN=.5,ENTRY_MS=ENTRY_MIN*60000,BARS_16H=16*60/ENTRY_MIN;
const BASE=`https://data.binance.vision/data/futures/um/daily/aggTrades/${SYMBOL}`;
const DOWNLOAD_CONCURRENCY=Math.max(1,Math.min(12,Number(process.env.R26_CONCURRENCY||6)));

function norm(v:number){if(v>1e17)return Math.floor(v/1e6);if(v>1e14)return Math.floor(v/1e3);return v;}
async function fetchDay(d:string):Promise<Bar[]>{const fn=`${SYMBOL}-aggTrades-${d}.zip`,r=await fetch(`${BASE}/${fn}`);if(r.status===404)return[];if(!r.ok||!r.body)throw new Error(`VISION_${r.status}:${d}`);const unzip=spawn('funzip',[],{stdio:['pipe','pipe','inherit']}),closed=new Promise<number|null>((resolve,reject)=>{unzip.once('error',reject);unzip.once('close',resolve)});Readable.fromWeb(r.body as never).pipe(unzip.stdin);const lines=readline.createInterface({input:unzip.stdout,crlfDelay:Infinity});const m=new Map<number,Bar>();for await(const line of lines){if(!line)continue;const c=line.split(','),p=Number(c[1]),q=Number(c[2]),t=norm(Number(c[5]));if(!Number.isFinite(p)||!Number.isFinite(q)||!Number.isFinite(t))continue;const k=Math.floor(t/ENTRY_MS)*ENTRY_MS;let b=m.get(k);if(!b){b={time:k,open:p,high:p,low:p,close:p,volume:0,buyVolume:0,sellVolume:0};m.set(k,b)}b.high=Math.max(b.high,p);b.low=Math.min(b.low,p);b.close=p;b.volume+=q;if(String(c[6]).trim().toLowerCase()==='true')b.sellVolume+=q;else b.buyVolume+=q;}const code=await closed;if(code!==0)throw new Error(`UNZIP_${code}:${d}`);return[...m.values()].sort((a,b)=>a.time-b.time)}
async function mapLimit<T,R>(xs:T[],limit:number,fn:(x:T,i:number)=>Promise<R>):Promise<R[]>{const out=new Array<R>(xs.length);let cursor=0;async function worker(){for(;;){const i=cursor++;if(i>=xs.length)return;out[i]=await fn(xs[i],i)}}await Promise.all(Array.from({length:Math.min(limit,xs.length)},worker));return out}
function buildSnapshots(b:Bar[]):Snap[]{const n=b.length,out=new Array<Snap>(n),sumC=new Float64Array(n+1),sumC2=new Float64Array(n+1),sumV=new Float64Array(n+1),sumBV=new Float64Array(n+1),sumSV=new Float64Array(n+1),sumPV=new Float64Array(n+1),sumTR=new Float64Array(n+1);const periods=[...new Set([...Array.from({length:10},(_,i)=>3+i),...Array.from({length:10},(_,i)=>10+i*2),20,50])],es=new Map<number,number>();const rangeSum=(a:Float64Array,l:number,r:number)=>a[r+1]-a[l];for(let i=0;i<n;i++){const x=b[i],c=x.close,v=Math.max(x.volume,1e-9),tr=i?Math.max(x.high-x.low,Math.abs(x.high-b[i-1].close),Math.abs(x.low-b[i-1].close)):x.high-x.low;sumC[i+1]=sumC[i]+c;sumC2[i+1]=sumC2[i]+c*c;sumV[i+1]=sumV[i]+x.volume;sumBV[i+1]=sumBV[i]+x.buyVolume;sumSV[i+1]=sumSV[i]+x.sellVolume;sumPV[i+1]=sumPV[i]+c*v;sumTR[i+1]=sumTR[i]+tr;for(const p of periods){const prev=es.get(p)??c;es.set(p,c*(2/(p+1))+prev*(1-2/(p+1)))}if(i<200){out[i]={buy:0,sell:0,buyFamilies:0,sellFamilies:0,atr:0,mom16:0};continue}let buy=0,sell=0,bf=0,sf=0;const add=(bit:number,side:Side|null)=>{if(side==='BUY'){buy++;bf|=bit}else if(side==='SELL'){sell++;sf|=bit}};for(let z=0;z<10;z++){const ef=es.get(3+z)!,slow=es.get(10+z*2)!;add(1<<0,ef>slow?'BUY':ef<slow?'SELL':null)}for(let z=0;z<10;z++){const p=2+z,roc=(c/b[i-p].close-1)*100,t=.008+z*.004;add(1<<1,roc>t?'BUY':roc<-t?'SELL':null)}for(let z=0;z<10;z++){const q=4+z;let h=-Infinity,l=Infinity;for(let j=i-q;j<i;j++){h=Math.max(h,b[j].high);l=Math.min(l,b[j].low)}add(1<<2,c>h?'BUY':c<l?'SELL':null)}for(let z=0;z<10;z++){const p=6+z;let g=0,l=0;for(let j=i-p+1;j<=i;j++){const d=b[j].close-b[j-1].close;if(d>0)g+=d;else l-=d}const rv=l?100-100/(1+g/l):100,u=53+z*.8,dn=47-z*.8;add(1<<3,rv>=u?'BUY':rv<=dn?'SELL':null)}for(let z=0;z<10;z++){const q=10+z,l=i-q+1,s=rangeSum(sumC,l,i),s2=rangeSum(sumC2,l,i),m=s/q,dev=Math.sqrt(Math.max(0,s2/q-m*m)),zz=dev?(c-m)/dev:0,t=.55+z*.1;add(1<<4,zz>=t?'BUY':zz<=-t?'SELL':null)}for(let z=0;z<10;z++){const q=2+z,l=i-q+1,bv=rangeSum(sumBV,l,i),sv=rangeSum(sumSV,l,i),tot=bv+sv,f=tot?(bv-sv)/tot:0,t=.05+z*.016;add(1<<5,f>=t?'BUY':f<=-t?'SELL':null)}for(let z=0;z<10;z++){const q=5+z,av=rangeSum(sumV,i-q,i-1)/q,r=av?x.volume/av:0,t=1.05+z*.1;add(1<<6,r>=t?(c>x.open?'BUY':c<x.open?'SELL':null):null)}const candleRange=Math.max(1e-9,x.high-x.low),body=Math.abs(c-x.open)/candleRange;for(let z=0;z<10;z++){const t=.35+z*.04;add(1<<7,body>=t&&c>b[i-1].close?'BUY':body>=t&&c<b[i-1].close?'SELL':null)}const atr14=rangeSum(sumTR,i-13,i)/14,e20=es.get(20)!,e50=es.get(50)!;for(let z=0;z<10;z++){const tol=atr14*(.15+z*.03);add(1<<8,Math.abs(c-e20)<=tol?(e20>e50?'BUY':e20<e50?'SELL':null):null)}for(let z=0;z<10;z++){const q=10+z*2,l=i-q+1,den=rangeSum(sumV,l,i),vw=den?rangeSum(sumPV,l,i)/den:c,pc=(c/vw-1)*100,t=.006+z*.003;add(1<<9,pc>=t?'BUY':pc<=-t?'SELL':null)}const j=i-BARS_16H,mom16=j>=0&&b[j].close?(c/b[j].close-1)*100:0;out[i]={buy,sell,buyFamilies:popcount(bf),sellFamilies:popcount(sf),atr:atr14,mom16};if(i%20000===0)console.log('SNAP',i,'/',n)}return out}
function popcount(x:number){let n=0;while(x){x&=x-1;n++}return n}

function consensusCandidates(snaps:Snap[],c:ConsensusCfg,start:number,end:number,useTsmom:boolean,momThreshold:number):Candidate[]{const out:Candidate[]=[];for(let i=Math.max(start,2000);i<end;i++){const s=snaps[i];let side:Side|null=null,cand=0,opp=0,fams=0;if(useTsmom){if(s.mom16>=momThreshold){side='BUY';cand=s.buy;opp=s.sell;fams=s.buyFamilies}else if(s.mom16<=-momThreshold){side='SELL';cand=s.sell;opp=s.buy;fams=s.sellFamilies}else continue;}else{if(s.buy>s.sell){side='BUY';cand=s.buy;opp=s.sell;fams=s.buyFamilies}else if(s.sell>s.buy){side='SELL';cand=s.sell;opp=s.buy;fams=s.sellFamilies}else continue;}if(cand>=c.minVotes&&fams>=c.minFamilies&&cand>=opp*c.lead)out.push({i,side});}return out;}

function simulate(b30:Bar[],snaps:Snap[],cands:Candidate[],holdMin:number,start:number,end:number):Stats{let eq=100,pk=100,dd=0,gp=0,gl=0,n=0,w=0,next=start;const holdBars=Math.max(1,Math.round(holdMin/ENTRY_MIN));for(const z of cands){const i=z.i;if(i<start||i>=end-holdBars-2||i<next)continue;const side=z.side,entry=b30[i+1].open,a=snaps[i].atr;if(!(a>0))continue;const target=Math.max(COST*2.8,a/entry*100*8),stop=Math.max(COST*1.25,target*.55),tp=side==='BUY'?entry*(1+target/100):entry*(1-target/100),sl=side==='BUY'?entry*(1-stop/100):entry*(1+stop/100);let exit=b30[i+holdBars].close,xi=i+holdBars;for(let j=i+1;j<=i+holdBars;j++){const y=b30[j],hs=side==='BUY'?y.low<=sl:y.high>=sl,ht=side==='BUY'?y.high>=tp:y.low<=tp;if(hs){exit=sl;xi=j;break}if(ht){exit=tp;xi=j;break}}const gross=(side==='BUY'?(exit/entry-1):(entry/exit-1))*100,net=gross-COST,ar=net*EXPOSURE;eq*=1+ar/100;pk=Math.max(pk,eq);dd=Math.max(dd,(pk-eq)/pk*100);n++;if(ar>0){w++;gp+=ar}else gl+=Math.abs(ar);next=xi+2;}const days=Math.max(1,(b30[Math.min(end-1,b30.length-1)].time-b30[Math.max(start,0)].time)/DAY);return{trades:n,winRate:n?w/n*100:0,pf:gl?gp/gl:gp?99:0,returnPct:eq-100,ddPct:dd,tradesPerDay:n/days,final50:eq/2};}
function score(s:Stats){if(s.trades<12)return-999;return s.returnPct-s.ddPct*.35+(s.pf-1)*2+Math.min(2,s.tradesPerDay*.035)}

async function main(){const end=Math.floor((Date.now()-2*DAY)/DAY)*DAY,start=end-DAYS*DAY,dates:string[]=[];for(let t=start;t<end;t+=DAY)dates.push(new Date(t).toISOString().slice(0,10));const daily=await mapLimit(dates,DOWNLOAD_CONCURRENCY,async d=>{const bars=await fetchDay(d);console.log('DAY',d,bars.length);return bars}),b30=daily.flat().sort((a,b)=>a.time-b.time),N=b30.length;if(N<2500)throw new Error(`INSUFFICIENT_BARS:${N}`);const t1=Math.floor(N*.5),t2=Math.floor(N*.75);console.log('PRECOMPUTE_START',N);const snaps=buildSnapshots(b30);console.log('PRECOMPUTE_DONE');
 const consensusCfgs:ConsensusCfg[]=[];for(const minVotes of [5,10,15,20,25])for(const minFamilies of [2,3,4,5])for(const lead of [1.0,1.1,1.2,1.35])consensusCfgs.push({minVotes,minFamilies,lead});
 const holds=[15,30,60,120],moms=[.3,.5,.6,.8,1.0,1.2];let rows:Array<{c:Cfg;tr:Stats;va:Stats;s:number}>=[];
 for(const cc of consensusCfgs){for(const mt of moms){const allT=consensusCandidates(snaps,cc,0,N,true,mt);for(const holdMin of holds){const c:Cfg={...cc,holdMin,mom16Threshold:mt},tr=simulate(b30,snaps,allT,holdMin,0,t1),va=simulate(b30,snaps,allT,holdMin,t1,t2);rows.push({c,tr,va,s:score(tr)+score(va)*1.5});}}}
 rows.sort((a,b)=>b.s-a.s);console.log('R23_TOP10',JSON.stringify(rows.slice(0,10)));const best=rows[0],bestCC={minVotes:best.c.minVotes,minFamilies:best.c.minFamilies,lead:best.c.lead},bestT=consensusCandidates(snaps,bestCC,0,N,true,best.c.mom16Threshold),test=simulate(b30,snaps,bestT,best.c.holdMin,t2,N),pureC=consensusCandidates(snaps,bestCC,0,N,false,0),pureTrain=simulate(b30,snaps,pureC,best.c.holdMin,0,t1),pureVal=simulate(b30,snaps,pureC,best.c.holdMin,t1,t2),pureTest=simulate(b30,snaps,pureC,best.c.holdMin,t2,N);
 console.log('R23_RESULT',JSON.stringify({symbol:SYMBOL,days:DAYS,bars:N,entryTimeframe:'30s',costPct:COST,risk:{initial:50,marginPct:1,leverage:10},architecture:'R23_30S_WINRATE_LAB',config:best.c,withTsmom:{train:best.tr,validation:best.va,test,survived:best.tr.returnPct>0&&best.va.returnPct>0&&test.returnPct>0&&best.va.pf>1&&test.pf>1},pureScalperSameConfig:{train:pureTrain,validation:pureVal,test:pureTest}}));}
function simulateR23(b:Bar[],cands:Candidate[],cfg:R23Cfg,start:number,end:number):Stats{let eq=100,pk=100,dd=0,gp=0,gl=0,n=0,w=0,next=start;const holdBars=Math.round(cfg.holdMin/ENTRY_MIN);for(const z of cands){const i=z.i;if(i<start||i>=end-holdBars-2||i<next)continue;const side:Side=cfg.reverse?(z.side==='BUY'?'SELL':'BUY'):z.side,entry=b[i+1].open,tp=side==='BUY'?entry*(1+cfg.targetPct/100):entry*(1-cfg.targetPct/100),sl=side==='BUY'?entry*(1-cfg.stopPct/100):entry*(1+cfg.stopPct/100);let exit=b[i+holdBars].close,xi=i+holdBars;for(let j=i+1;j<=i+holdBars;j++){const y=b[j],hitStop=side==='BUY'?y.low<=sl:y.high>=sl,hitTarget=side==='BUY'?y.high>=tp:y.low<=tp;if(hitStop){exit=sl;xi=j;break}if(hitTarget){exit=tp;xi=j;break}}const net=(side==='BUY'?(exit/entry-1):(entry/exit-1))*100-COST,ar=net*EXPOSURE;eq*=1+ar/100;pk=Math.max(pk,eq);dd=Math.max(dd,(pk-eq)/pk*100);n++;if(ar>0){w++;gp+=ar}else gl+=Math.abs(ar);next=xi+2}const days=Math.max(1,(b[Math.min(end-1,b.length-1)].time-b[Math.max(start,0)].time)/DAY);return{trades:n,winRate:n?w/n*100:0,pf:gl?gp/gl:gp?99:0,returnPct:eq-100,ddPct:dd,tradesPerDay:n/days,final50:eq/2}}
function passesR23(s:Stats){return s.winRate>=65&&s.tradesPerDay>=10&&s.pf>=1.2&&s.returnPct>0&&s.trades>=150}
function r23Score(s:Stats){return s.returnPct*3+s.pf*4+s.winRate*.08+s.tradesPerDay*.08-s.ddPct*2-Math.max(0,65-s.winRate)*1.5-Math.max(0,10-s.tradesPerDay)*2}

async function mainR23(){const end=Math.floor((Date.now()-2*DAY)/DAY)*DAY,start=end-DAYS*DAY,dates:string[]=[];for(let t=start;t<end;t+=DAY)dates.push(new Date(t).toISOString().slice(0,10));const daily=await mapLimit(dates,DOWNLOAD_CONCURRENCY,async d=>{const bars=await fetchDay(d);console.log('DAY',d,bars.length);return bars}),bars=daily.flat().sort((a,b)=>a.time-b.time),N=bars.length;if(N<4000)throw new Error(`INSUFFICIENT_BARS:${N}`);const t1=Math.floor(N*.5),t2=Math.floor(N*.75),snaps=buildSnapshots(bars);console.log('R23_SEARCH_START',N);const rows:Array<{cfg:R23Cfg;train:Stats;validation:Stats;score:number;qualified:boolean}>=[];for(const minVotes of [10,20,30])for(const minFamilies of [2,4])for(const mom16Threshold of [.15,.3,.6,1]){const base={minVotes,minFamilies,lead:1.15},cands=consensusCandidates(snaps,base,0,N,true,mom16Threshold);for(const holdMin of [2,5,10])for(const targetPct of [.24,.32,.42,.55])for(const stopPct of [.15,.2,.28,.38])for(const reverse of [false,true]){const cfg:R23Cfg={...base,mom16Threshold,holdMin,targetPct,stopPct,reverse},train=simulateR23(bars,cands,cfg,0,t1),validation=simulateR23(bars,cands,cfg,t1,t2),qualified=passesR23(train)&&passesR23(validation),score=r23Score(train)+r23Score(validation)*1.5+(qualified?10000:0);rows.push({cfg,train,validation,score,qualified})}}rows.sort((a,b)=>b.score-a.score);console.log('R23_TOP10',JSON.stringify(rows.slice(0,10)));const best=rows[0],bestCands=consensusCandidates(snaps,best.cfg,0,N,true,best.cfg.mom16Threshold),test=simulateR23(bars,bestCands,best.cfg,t2,N),survived=best.qualified&&passesR23(test);console.log('R23_RESULT',JSON.stringify({symbol:SYMBOL,days:DAYS,bars:N,entryTimeframe:'30s',costPct:COST,selection:'TRAIN_AND_VALIDATION_ONLY_BLIND_TEST',requirements:{winRateMin:65,tradesPerDayMin:10,pfMin:1.2,testTradesMin:150,positiveReturn:true},config:best.cfg,train:best.train,validation:best.validation,test,survived,qualifiedTrainValidation:best.qualified,qualifiedConfigurations:rows.filter(x=>x.qualified).length}))}
// R23 entrypoint disabled in the structural laboratory.

type PivotKind='SUPPORT'|'RESISTANCE';
type PivotEvent={knownAt:number;price:number;kind:PivotKind;tf:string};
type Structure={support:number;resistance:number;supportTf:string;resistanceTf:string};
type StructuralCfg=ConsensusCfg&{mom16Threshold:number;holdMin:number;minNetRr:number;maxRiskPct:number;atrBuffer:number};
type TradeAudit={signalTime:string;entryTime:string;side:Side;entry:number;support:number;resistance:number;supportTf:string;resistanceTf:string;sl:number;tp:number;riskPct:number;rewardPct:number;netRr:number;exitTime:string;exit:number;netPct:number;outcome:'WIN'|'LOSS'};

function reaggregate(input:Bar[],ms:number){const out:Bar[]=[];let current:Bar|undefined;for(const x of input){const time=Math.floor(x.time/ms)*ms;if(!current||current.time!==time){current={...x,time};out.push(current)}else{current.high=Math.max(current.high,x.high);current.low=Math.min(current.low,x.low);current.close=x.close;current.volume+=x.volume;current.buyVolume+=x.buyVolume;current.sellVolume+=x.sellVolume}}return out}
function pivotEvents(bars:Bar[],ms:number,tf:string){const h=reaggregate(bars,ms),events:PivotEvent[]=[];for(let i=2;i<h.length-2;i++){const knownAt=h[i+2].time+ms,p=h[i];if(p.low<h[i-2].low&&p.low<=h[i-1].low&&p.low<h[i+1].low&&p.low<=h[i+2].low)events.push({knownAt,price:p.low,kind:'SUPPORT',tf});if(p.high>h[i-2].high&&p.high>=h[i-1].high&&p.high>h[i+1].high&&p.high>=h[i+2].high)events.push({knownAt,price:p.high,kind:'RESISTANCE',tf})}return events}
function buildStructures(bars:Bar[]){const events=[...pivotEvents(bars,60000,'1m'),...pivotEvents(bars,300000,'5m'),...pivotEvents(bars,900000,'15m'),...pivotEvents(bars,3600000,'1h')].sort((a,b)=>a.knownAt-b.knownAt),out=new Array<Structure>(bars.length),active:Record<string,{s:PivotEvent[];r:PivotEvent[]}>={'1m':{s:[],r:[]},'5m':{s:[],r:[]},'15m':{s:[],r:[]},'1h':{s:[],r:[]}};let e=0;for(let i=0;i<bars.length;i++){while(e<events.length&&events[e].knownAt<=bars[i].time){const x=events[e++],a=active[x.tf],list=x.kind==='SUPPORT'?a.s:a.r;list.push(x);if(list.length>24)list.shift()}const price=bars[i].close;let support=0,resistance=Infinity,supportTf='',resistanceTf='';for(const tf of ['1m','5m','15m','1h']){for(const x of active[tf].s)if(x.price<price&&x.price>support){support=x.price;supportTf=tf}for(const x of active[tf].r)if(x.price>price&&x.price<resistance){resistance=x.price;resistanceTf=tf}}out[i]={support,resistance:Number.isFinite(resistance)?resistance:0,supportTf,resistanceTf}}return out}
function structuralSim(bars:Bar[],snaps:Snap[],structures:Structure[],cands:Candidate[],cfg:StructuralCfg,start:number,end:number,audit=false){let eq=100,pk=100,dd=0,gp=0,gl=0,n=0,w=0,next=start,rejectedNoLevels=0,rejectedSpace=0;const trades:TradeAudit[]=[],holdBars=Math.round(cfg.holdMin/ENTRY_MIN);for(const z of cands){const i=z.i;if(i<start||i>=end-holdBars-2||i<next)continue;const side=z.side,entry=bars[i+1].open,a=snaps[i].atr,s=structures[i];if(!(a>0&&s.support>0&&s.resistance>0)){rejectedNoLevels++;continue}const buffer=a*cfg.atrBuffer,sl=side==='BUY'?s.support-buffer:s.resistance+buffer,tp=side==='BUY'?s.resistance-buffer:s.support+buffer;if(side==='BUY'&&!(sl<entry&&tp>entry)||side==='SELL'&&!(sl>entry&&tp<entry)){rejectedSpace++;continue}const riskPct=Math.abs(entry-sl)/entry*100,rewardPct=Math.abs(tp-entry)/entry*100,netRr=(rewardPct-COST)/(riskPct+COST);if(rewardPct<=COST*1.25||riskPct>cfg.maxRiskPct||netRr<cfg.minNetRr){rejectedSpace++;continue}let exit=bars[i+holdBars].close,xi=i+holdBars;for(let j=i+1;j<=i+holdBars;j++){const y=bars[j],hitStop=side==='BUY'?y.low<=sl:y.high>=sl,hitTarget=side==='BUY'?y.high>=tp:y.low<=tp;if(hitStop){exit=sl;xi=j;break}if(hitTarget){exit=tp;xi=j;break}}const net=(side==='BUY'?(exit/entry-1):(entry/exit-1))*100-COST,ar=net*EXPOSURE;eq*=1+ar/100;pk=Math.max(pk,eq);dd=Math.max(dd,(pk-eq)/pk*100);n++;if(ar>0){w++;gp+=ar}else gl+=Math.abs(ar);if(audit)trades.push({signalTime:new Date(bars[i].time).toISOString(),entryTime:new Date(bars[i+1].time).toISOString(),side,entry,support:s.support,resistance:s.resistance,supportTf:s.supportTf,resistanceTf:s.resistanceTf,sl,tp,riskPct,rewardPct,netRr,exitTime:new Date(bars[xi].time).toISOString(),exit,netPct:net,outcome:net>0?'WIN':'LOSS'});next=xi+2}const days=Math.max(1,(bars[Math.min(end-1,bars.length-1)].time-bars[Math.max(start,0)].time)/DAY),stats:Stats={trades:n,winRate:n?w/n*100:0,pf:gl?gp/gl:gp?99:0,returnPct:eq-100,ddPct:dd,tradesPerDay:n/days,final50:eq/2};return{stats,trades,rejectedNoLevels,rejectedSpace}}
function structuralPass(s:Stats){return s.winRate>=65&&s.tradesPerDay>=10&&s.pf>=1.2&&s.returnPct>0&&s.trades>=150}
function structuralScore(s:Stats){return s.returnPct*4+s.pf*5+s.winRate*.12+s.tradesPerDay*.12-s.ddPct*2-Math.max(0,65-s.winRate)*1.6-Math.max(0,10-s.tradesPerDay)*2.5}

async function mainR24(){const end=Math.floor((Date.now()-2*DAY)/DAY)*DAY,start=end-DAYS*DAY,dates:string[]=[];for(let t=start;t<end;t+=DAY)dates.push(new Date(t).toISOString().slice(0,10));const daily=await mapLimit(dates,DOWNLOAD_CONCURRENCY,async d=>{const x=await fetchDay(d);console.log('DAY',d,x.length);return x}),bars=daily.flat().sort((a,b)=>a.time-b.time),N=bars.length,t1=Math.floor(N*.5),t2=Math.floor(N*.75);if(N<4000)throw new Error(`INSUFFICIENT_BARS:${N}`);console.log('R24_PRECOMPUTE',N);const snaps=buildSnapshots(bars),structures=buildStructures(bars),rows:Array<{cfg:StructuralCfg;train:Stats;validation:Stats;score:number;qualified:boolean}>=[];for(const minVotes of [10,20,30])for(const minFamilies of [2,4])for(const mom16Threshold of [.3,.6,1]){const base={minVotes,minFamilies,lead:1.15},cands=consensusCandidates(snaps,base,0,N,true,mom16Threshold);for(const holdMin of [15,30,60])for(const minNetRr of [.8,1,1.25])for(const maxRiskPct of [.25,.4,.65])for(const atrBuffer of [.1,.2]){const cfg={...base,mom16Threshold,holdMin,minNetRr,maxRiskPct,atrBuffer},train=structuralSim(bars,snaps,structures,cands,cfg,0,t1).stats,validation=structuralSim(bars,snaps,structures,cands,cfg,t1,t2).stats,qualified=structuralPass(train)&&structuralPass(validation),score=r25Score(train)+r25Score(validation)*1.5+(qualified?10000:0);rows.push({cfg,train,validation,score,qualified})}}rows.sort((a,b)=>b.score-a.score);const best=rows[0],cands=consensusCandidates(snaps,best.cfg,0,N,true,best.cfg.mom16Threshold),blind=structuralSim(bars,snaps,structures,cands,best.cfg,t2,N,true),survived=best.qualified&&structuralPass(blind.stats);await mkdir('artifacts',{recursive:true});await writeFile('artifacts/r24-trades.json',JSON.stringify({config:best.cfg,summary:blind.stats,trades:blind.trades},null,2));console.log('R24_TOP10',JSON.stringify(rows.slice(0,10)));console.log('R24_RESULT',JSON.stringify({symbol:SYMBOL,days:DAYS,bars:N,entryTimeframe:'30s',structureTimeframes:['1m','5m','15m','1h'],lookaheadFree:'pivots confirmed after two closed candles',costPct:COST,requirements:{winRateMin:65,tradesPerDayMin:10,pfMin:1.2,testTradesMin:150},config:best.cfg,train:best.train,validation:best.validation,test:blind.stats,rejected:{noLevels:blind.rejectedNoLevels,insufficientSpaceOrRisk:blind.rejectedSpace},qualifiedConfigurations:rows.filter(x=>x.qualified).length,survived,auditFile:'artifacts/r24-trades.json'}))}
// R24 entrypoint disabled in the structure-entry laboratory.

type R25Cfg=StructuralCfg&{proximityAtr:number;voteLead:number;flowLead:number};
function r25Score(s:Stats){return s.returnPct*4+Math.min(s.pf,3)*5+s.winRate*.12+s.tradesPerDay*.12-s.ddPct*2-Math.max(0,65-s.winRate)*1.6-Math.max(0,10-s.tradesPerDay)*2.5-Math.max(0,100-s.trades)*.6}
function structureEntryCandidates(bars:Bar[],snaps:Snap[],structures:Structure[],cfg:R25Cfg){const out:Candidate[]=[];for(let i=2000;i<bars.length-2;i++){const x=bars[i],s=structures[i],a=snaps[i].atr;if(!(a>0&&s.support>0&&s.resistance>0))continue;const nearSupport=x.low<=s.support+a*cfg.proximityAtr&&x.close>s.support,nearResistance=x.high>=s.resistance-a*cfg.proximityAtr&&x.close<s.resistance,bullReject=x.close>x.open&&x.close>=(x.low+x.high)/2&&x.buyVolume>=x.sellVolume*cfg.flowLead,bearReject=x.close<x.open&&x.close<=(x.low+x.high)/2&&x.sellVolume>=x.buyVolume*cfg.flowLead,buyVotes=snaps[i].buy>=Math.max(5,snaps[i].sell*cfg.voteLead),sellVotes=snaps[i].sell>=Math.max(5,snaps[i].buy*cfg.voteLead),trendBuy=snaps[i].mom16>=cfg.mom16Threshold,trendSell=snaps[i].mom16<=-cfg.mom16Threshold;const continuationBuy=trendBuy&&nearSupport&&bullReject&&buyVotes,continuationSell=trendSell&&nearResistance&&bearReject&&sellVotes,rangeBuy=Math.abs(snaps[i].mom16)<cfg.mom16Threshold&&nearSupport&&bullReject,rangeSell=Math.abs(snaps[i].mom16)<cfg.mom16Threshold&&nearResistance&&bearReject;if(continuationBuy||rangeBuy)out.push({i,side:'BUY'});else if(continuationSell||rangeSell)out.push({i,side:'SELL'})}return out}

async function mainR25(){const end=Math.floor((Date.now()-2*DAY)/DAY)*DAY,start=end-DAYS*DAY,dates:string[]=[];for(let t=start;t<end;t+=DAY)dates.push(new Date(t).toISOString().slice(0,10));const daily=await mapLimit(dates,DOWNLOAD_CONCURRENCY,async d=>{const x=await fetchDay(d);console.log('DAY',d,x.length);return x}),bars=daily.flat().sort((a,b)=>a.time-b.time),N=bars.length,t1=Math.floor(N*.5),t2=Math.floor(N*.75);if(N<4000)throw new Error(`INSUFFICIENT_BARS:${N}`);console.log('R25_PRECOMPUTE',N);const snaps=buildSnapshots(bars),structures=buildStructures(bars),rows:Array<{cfg:R25Cfg;train:Stats;validation:Stats;score:number;qualified:boolean;candidates:number}>=[];for(const mom16Threshold of [.3,.6,1])for(const proximityAtr of [.25,.5,1,1.5])for(const voteLead of [1,1.15])for(const flowLead of [1,1.1]){const seed:R25Cfg={minVotes:0,minFamilies:0,lead:0,mom16Threshold,proximityAtr,voteLead,flowLead,holdMin:15,minNetRr:.8,maxRiskPct:.5,atrBuffer:.1},cands=structureEntryCandidates(bars,snaps,structures,seed);for(const holdMin of [5,15,30])for(const minNetRr of [.5,.8,1.1])for(const maxRiskPct of [.25,.5,.8])for(const atrBuffer of [.05,.15]){const cfg={...seed,holdMin,minNetRr,maxRiskPct,atrBuffer},train=structuralSim(bars,snaps,structures,cands,cfg,0,t1).stats,validation=structuralSim(bars,snaps,structures,cands,cfg,t1,t2).stats,qualified=structuralPass(train)&&structuralPass(validation),score=r25Score(train)+r25Score(validation)*1.5+(qualified?10000:0);rows.push({cfg,train,validation,score,qualified,candidates:cands.length})}}rows.sort((a,b)=>b.score-a.score);const best=rows[0],cands=structureEntryCandidates(bars,snaps,structures,best.cfg),blind=structuralSim(bars,snaps,structures,cands,best.cfg,t2,N,true),survived=best.qualified&&structuralPass(blind.stats);await mkdir('artifacts',{recursive:true});await writeFile('artifacts/r25-trades.json',JSON.stringify({config:best.cfg,summary:blind.stats,trades:blind.trades},null,2));console.log('R25_TOP10',JSON.stringify(rows.slice(0,10)));console.log('R25_RESULT',JSON.stringify({symbol:SYMBOL,days:DAYS,bars:N,entryTimeframe:'30s',entryEngines:['TREND_PULLBACK_REJECTION','RANGE_LEVEL_REJECTION'],structureTimeframes:['1m','5m','15m','1h'],lookaheadFree:true,costPct:COST,requirements:{winRateMin:65,tradesPerDayMin:10,pfMin:1.2,testTradesMin:150},config:best.cfg,candidates:best.candidates,train:best.train,validation:best.validation,test:blind.stats,rejected:{noLevels:blind.rejectedNoLevels,insufficientSpaceOrRisk:blind.rejectedSpace},qualifiedConfigurations:rows.filter(x=>x.qualified).length,survived,auditFile:'artifacts/r25-trades.json'}))}
// R25 entrypoint disabled in the split-structure laboratory.

function buildSelectedStructures(bars:Bar[],selected:Array<{ms:number;tf:string}>){const events=selected.flatMap(x=>pivotEvents(bars,x.ms,x.tf)).sort((a,b)=>a.knownAt-b.knownAt),out=new Array<Structure>(bars.length),active=new Map<string,{s:PivotEvent[];r:PivotEvent[]}>();for(const x of selected)active.set(x.tf,{s:[],r:[]});let e=0;for(let i=0;i<bars.length;i++){while(e<events.length&&events[e].knownAt<=bars[i].time){const x=events[e++],a=active.get(x.tf)!,list=x.kind==='SUPPORT'?a.s:a.r;list.push(x);if(list.length>30)list.shift()}const price=bars[i].close;let support=0,resistance=Infinity,supportTf='',resistanceTf='';for(const {tf} of selected){const a=active.get(tf)!;for(const x of a.s)if(x.price<price&&x.price>support){support=x.price;supportTf=tf}for(const x of a.r)if(x.price>price&&x.price<resistance){resistance=x.price;resistanceTf=tf}}out[i]={support,resistance:Number.isFinite(resistance)?resistance:0,supportTf,resistanceTf}}return out}
function splitStructureSim(bars:Bar[],snaps:Snap[],entryStructure:Structure[],targetStructure:Structure[],cands:Candidate[],cfg:R25Cfg,start:number,end:number,audit=false){let eq=100,pk=100,dd=0,gp=0,gl=0,n=0,w=0,next=start,rejectedNoLevels=0,rejectedSpace=0;const trades:TradeAudit[]=[],holdBars=Math.round(cfg.holdMin/ENTRY_MIN);for(const z of cands){const i=z.i;if(i<start||i>=end-holdBars-2||i<next)continue;const side=z.side,entry=bars[i+1].open,a=snaps[i].atr,local=entryStructure[i],macro=targetStructure[i];if(!(a>0&&local.support>0&&local.resistance>0&&macro.support>0&&macro.resistance>0)){rejectedNoLevels++;continue}const buffer=a*cfg.atrBuffer,sl=side==='BUY'?local.support-buffer:local.resistance+buffer,tp=side==='BUY'?macro.resistance-buffer:macro.support+buffer;if(side==='BUY'&&!(sl<entry&&tp>entry)||side==='SELL'&&!(sl>entry&&tp<entry)){rejectedSpace++;continue}const riskPct=Math.abs(entry-sl)/entry*100,rewardPct=Math.abs(tp-entry)/entry*100,netRr=(rewardPct-COST)/(riskPct+COST);if(rewardPct<=COST*1.25||riskPct>cfg.maxRiskPct||netRr<cfg.minNetRr){rejectedSpace++;continue}let exit=bars[i+holdBars].close,xi=i+holdBars;for(let j=i+1;j<=i+holdBars;j++){const y=bars[j],hitStop=side==='BUY'?y.low<=sl:y.high>=sl,hitTarget=side==='BUY'?y.high>=tp:y.low<=tp;if(hitStop){exit=sl;xi=j;break}if(hitTarget){exit=tp;xi=j;break}}const net=(side==='BUY'?(exit/entry-1):(entry/exit-1))*100-COST,ar=net*EXPOSURE;eq*=1+ar/100;pk=Math.max(pk,eq);dd=Math.max(dd,(pk-eq)/pk*100);n++;if(ar>0){w++;gp+=ar}else gl+=Math.abs(ar);if(audit)trades.push({signalTime:new Date(bars[i].time).toISOString(),entryTime:new Date(bars[i+1].time).toISOString(),side,entry,support:local.support,resistance:macro.resistance,supportTf:local.supportTf,resistanceTf:macro.resistanceTf,sl,tp,riskPct,rewardPct,netRr,exitTime:new Date(bars[xi].time).toISOString(),exit,netPct:net,outcome:net>0?'WIN':'LOSS'});next=xi+2}const days=Math.max(1,(bars[Math.min(end-1,bars.length-1)].time-bars[Math.max(start,0)].time)/DAY),stats:Stats={trades:n,winRate:n?w/n*100:0,pf:gl?gp/gl:gp?99:0,returnPct:eq-100,ddPct:dd,tradesPerDay:n/days,final50:eq/2};return{stats,trades,rejectedNoLevels,rejectedSpace}}

async function mainR26(){const end=Math.floor((Date.now()-2*DAY)/DAY)*DAY,start=end-DAYS*DAY,dates:string[]=[];for(let t=start;t<end;t+=DAY)dates.push(new Date(t).toISOString().slice(0,10));const daily=await mapLimit(dates,DOWNLOAD_CONCURRENCY,async d=>{const x=await fetchDay(d);console.log('DAY',d,x.length);return x}),bars=daily.flat().sort((a,b)=>a.time-b.time),N=bars.length,t1=Math.floor(N*.5),t2=Math.floor(N*.75);if(N<4000)throw new Error(`INSUFFICIENT_BARS:${N}`);console.log('R26_PRECOMPUTE',N);const snaps=buildSnapshots(bars),entryStructure=buildSelectedStructures(bars,[{ms:60000,tf:'1m'},{ms:300000,tf:'5m'}]),targetStructure=buildSelectedStructures(bars,[{ms:300000,tf:'5m'},{ms:900000,tf:'15m'},{ms:3600000,tf:'1h'}]),rows:Array<{cfg:R25Cfg;train:Stats;validation:Stats;score:number;qualified:boolean;candidates:number}>=[];for(const mom16Threshold of [.3,.6,1])for(const proximityAtr of [.25,.5,1,1.5])for(const voteLead of [1,1.15])for(const flowLead of [1,1.1]){const seed:R25Cfg={minVotes:0,minFamilies:0,lead:0,mom16Threshold,proximityAtr,voteLead,flowLead,holdMin:30,minNetRr:.8,maxRiskPct:.5,atrBuffer:.1},cands=structureEntryCandidates(bars,snaps,entryStructure,seed);for(const holdMin of [15,30,60,120])for(const minNetRr of [.5,.8,1.1])for(const maxRiskPct of [.25,.5,.8,1.2])for(const atrBuffer of [.05,.15]){const cfg={...seed,holdMin,minNetRr,maxRiskPct,atrBuffer},train=splitStructureSim(bars,snaps,entryStructure,targetStructure,cands,cfg,0,t1).stats,validation=splitStructureSim(bars,snaps,entryStructure,targetStructure,cands,cfg,t1,t2).stats,qualified=structuralPass(train)&&structuralPass(validation),score=r25Score(train)+r25Score(validation)*1.5+(qualified?10000:0);rows.push({cfg,train,validation,score,qualified,candidates:cands.length})}}rows.sort((a,b)=>b.score-a.score);const best=rows[0],cands=structureEntryCandidates(bars,snaps,entryStructure,best.cfg),blind=splitStructureSim(bars,snaps,entryStructure,targetStructure,cands,best.cfg,t2,N,true),survived=best.qualified&&structuralPass(blind.stats);await mkdir('artifacts',{recursive:true});await writeFile('artifacts/r26-trades.json',JSON.stringify({config:best.cfg,summary:blind.stats,trades:blind.trades},null,2));console.log('R26_TOP10',JSON.stringify(rows.slice(0,10)));console.log('R26_RESULT',JSON.stringify({symbol:SYMBOL,days:DAYS,bars:N,entryTimeframe:'30s',entryAndStopStructure:['1m','5m'],targetStructure:['5m','15m','1h'],lookaheadFree:true,costPct:COST,requirements:{winRateMin:65,tradesPerDayMin:10,pfMin:1.2,testTradesMin:150},config:best.cfg,candidates:best.candidates,train:best.train,validation:best.validation,test:blind.stats,rejected:{noLevels:blind.rejectedNoLevels,insufficientSpaceOrRisk:blind.rejectedSpace},qualifiedConfigurations:rows.filter(x=>x.qualified).length,survived,auditFile:'artifacts/r26-trades.json'}))}
mainR26().catch(e=>{console.error('R26_ERROR',e instanceof Error?e.message:String(e));process.exit(1)});
