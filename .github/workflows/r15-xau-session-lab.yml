name: R15 XAU Session Lab

on:
  workflow_dispatch:
  push:
    branches:
      - feature/v34-dual-market-engine
    paths:
      - 'backend/src/r15XauSessionMomentumLab.ts'
      - '.github/workflows/r15-xau-session-lab.yml'

jobs:
  session-lab:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npx tsx src/r15XauSessionMomentumLab.ts
